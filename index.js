const arrayNums = [1, 2, 3, 4];

const numMult = arrayNums.map((multiplicacao)); 

function multiplicacao(num){
    return num * 20;
}

let aleatorio = Math.floor(Math.random() * 20);
let mult = 12 * 19;

console.log(mult);